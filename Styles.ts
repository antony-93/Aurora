import { StyleSheet } from "react-native";

export const { rowVerticalCenter } = StyleSheet.create({
    rowVerticalCenter: {
        display: "flex",
        flexDirection: 'row',
        alignItems: 'center'
    }
})